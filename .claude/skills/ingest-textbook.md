# Textbook Ingestion Compatibility Entry

Use the canonical project skill:

`../../.agents/skills/medlearn-review-ingestion/SKILL.md`

The production repository root is `F:\ml`. Read `AGENTS.md`,
`context/TASK_ROUTER.yaml`, `docs/PIPELINE_INDEX.md`,
`manifests/internal_medicine_ingestion.yaml`, and
`state/knowledge_ingestion.yaml` before acting.

Apply the checklists named in the canonical skill.

Do not use historical workspace paths or deprecated ingestion scripts.
