/**
 * 移动端紧凑布局参数
 * 控制页面边距、列表行高、卡片内边距，避免大屏设计稿比例直接套用到手机上。
 */
export const Layout = {
  screenPaddingX: 16,
  screenPaddingBottom: 32,
  sectionGap: 16,
  cardPadding: 12,
  cardRadius: 12,
  listRowHeight: 60,
  listRowHeightCompact: 52,
  iconWrap: 36,
  iconWrapSm: 32,
  heroPadding: 16,
} as const