# 协作记忆

本文件只记录长期协作偏好和执行原则。项目当前状态、训练结果、评估报告、
active object、adapter 路径等动态信息不要写进这里。

## 交流语言

- 与用户的所有交流统一使用中文。
- 工作进度、分析说明、方案、问题诊断和最终答复均使用中文。
- 代码注释、文档说明、目录说明、提交说明等面向人阅读的文字默认使用中文。
- 代码标识符、命令、文件名、接口名及必要的专业术语可以保留英文。
- 表达应直接、清楚，避免无必要地切换为英文。

## 协作偏好

- 默认把任务推进到可验证结果，不停在空泛方案或“你可以……”。
- 协作目标不是一次性回答，而是持续搭建让 Agent 更会工作的脚手架：上下文、工具、流程、偏好和验收标准。
- 遇到重复任务或隐性判断时，优先沉淀为文档、脚本、Skill、测试或清单，形成上下文复利。
- 汇报时说明“做了什么”和“这对项目有什么意义”。
- 解释要直接。发现严重问题、错误前提或风险时，要明确指出，不绕弯。
- 优先用真实项目数据验证关键链路；轻量问题用最小测试，关键管道再补真实样本回归。
- 文件和文档要保持清楚边界，避免根目录堆临时结果、旧备份和过期报告。
- 训练、PDF 解析、模型抽取、评估和 production 发布要分清职责，不把问题混在一起。

## 执行原则

- 在不破坏生产、不覆盖权威文件、不删除不可恢复内容的前提下，默认主动执行。
- 涉及删除、覆盖、production 写入、schema 迁移、训练覆盖、模型替换或数据库迁移时，必须先停下来确认。
- 严重问题要按同一因果链路系统修复，包括必要的解析、转换、评估、门禁、测试和文档。
- 系统修复不是无限扩展。无关重构、顺手美化和新功能另列后续任务。
- 生产安全优先。测试不过、评估不过、证据链不稳时，不为了推进而上线。
- 代码风格尊重现有项目；新增面向人的注释默认中文，但不为了翻译而大面积改动稳定代码。

## 状态记录规则

- `MEMORY.md` 只放长期协作偏好和执行边界。
- `docs/` 放权威设计文档、ADR、PRD、项目宪法和长期架构说明。
- `state/` 放当前状态、active object、验收状态和可恢复执行状态。
- `reports/` 放需要长期留存的评估报告、审计报告和人工可读汇总。
- `artifacts/` 放可重新生成的实验输出、导出文件、临时分析和 QA 产物。
- `archive/` 放废弃旧版本和历史备份，不作为当前事实来源。
- `scripts/` 放可重复执行脚本，不放手工状态记录。
- 当前训练阶段、报告路径、production 冻结状态、adapter 路径和某次 eval 结果属于动态状态，应写入对应的 `state/`、`reports/`、`training/reports/` 或权威文档索引，不写入本文件。
