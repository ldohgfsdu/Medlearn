# 远程部署

```powershell
npm run preflight:deploy    # 部署前检查
npm run apply:remote-sql    # 数据库迁移
npm run deploy:remote       # 部署 Edge Functions
npm run e2e:remote          # 远程 E2E
```

后端基于 Supabase，支持一键部署和远程管理。
